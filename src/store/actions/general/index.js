import * as types from "../../constants";

const epicClearStore = () => ({
  type: types.GENERAL.EPIC_CLEAR_STORE,
});

const epics = {
    epicClearStore
}

const general ={
    epics
}

export default general